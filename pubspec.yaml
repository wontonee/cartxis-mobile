name: vortex_app
description: Cartxis eCommerce Mobile Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  http: ^1.2.0
  intl: ^0.19.0
  razorpay_flutter: ^1.4.1
  flutter_stripe: ^11.2.0
  image_picker: ^1.0.7
  phonepe_payment_sdk: ^3.0.2
  crypto: ^3.0.7
  webview_flutter: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices.
  flutter_lints: ^6.0.0

# Flutter configuration
flutter:
  uses-material-design: true

  # Assets
  assets:
    - assets/logo.png
    - assets/transparent_logo.png
  # assets:
  #   - assets/images/
  #   - assets/icons/
  #   - assets/animations/

  # Fonts (Inter font family for design system)
  # Download Inter font from: https://fonts.google.com/specimen/Inter
  # Place font files in assets/fonts/ directory
  # fonts:
  #   - family: Inter
  #     fonts:
  #       - asset: assets/fonts/Inter-Regular.ttf
  #       - asset: assets/fonts/Inter-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/Inter-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/Inter-Bold.ttf
  #         weight: 700
  #       - asset: assets/fonts/Inter-ExtraBold.ttf
  #         weight: 800

